function run (number1, number2) {
	
	
	console.log ('run 100 miles per hour')
	
	return  // this returns an operation of a function. it must be in the body of a function. The return statement is usually at the bottom of the function. the return statement can return anything you want.
	
}

console.log (run(23, 26));

