function greet() {
    console.log('hi');   
}

greet.language = 'english';
console.log(greet.language);


/*First class functions: everything you can do with other types yoiu can do with functions.  You can assign them to a variable, pass them around, create them on the fly.
Functions are actually objects in Javascript, so using the dot nontation we can create a property.

Properties are added to function.  Here we have used the dot operator to find that the property in memeory which is attched to the function. Just like we can attach a property  to any object.

*/


